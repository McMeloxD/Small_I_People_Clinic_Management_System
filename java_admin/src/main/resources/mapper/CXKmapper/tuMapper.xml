<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace关联fxml文件和接口文件   charge_administration-->
<mapper namespace="com.example.mapper.CXKmapper.tuMapper">

    <insert id="addTu">
        insert into tu (avatar) values (#{avatar})
    </insert>
    <select id="findAll" resultType="com.example.entity.CXKentity.Tu">
        select * from tu
    </select>
</mapper>